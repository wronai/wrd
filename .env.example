# User and group IDs (run `id -u` and `id -g` to get your values)
UID=1000
GID=1000

# Timezone (e.g., America/New_York, Europe/Paris)
TZ=UTC

# VS Code web interface password
VSCODE_PASSWORD=your_secure_password_here

# Sudo password inside the container (for development)
SUDO_PASSWORD=password

# Claude API Key (get from https://console.anthropic.com/)
# ANTHROPIC_API_KEY=your_api_key_here
